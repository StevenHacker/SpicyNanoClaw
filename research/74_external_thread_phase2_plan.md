# External Thread Phase 2 Plan

## Purpose

This note defines the next parallel research wave for other main threads after dispatcher cycle 004 closed.

Phase 2 is no longer about finding the SNC spine.
That spine is already established.

Phase 2 is about:

- turning accepted host/donor evidence into design-grade packets
- finishing the most useful non-core breadth packets
- keeping parallel threads focused on work that directly improves SNC landing quality

## Current Read

Status:

- current numbered phase-2 packet set is complete

After accepted cycle-005 packets and the final breadth acceptances, the most strategically valuable external work has shifted from numbered packet completion to future packet creation and implementation support.

1. design packets that are newly unlocked by accepted evidence
2. broader CC/OpenClaw packets that complete productization and donor-separation context

The immediate unlocked design lanes are:

- durable memory v1
- multi-worker orchestration v1
- tool shaping / helper tools v1

Those now have enough evidence to be worth parallelizing.

## Recommended Launch Waves

### Wave A: Design-Grade SNC Packets

Status:

- completed

Completed packets:

1. `14` SNC-10 Durable Memory V1 Design Packet
2. `16` SNC-12 Multi-Worker Orchestration V1 Design Packet
3. `17` SNC-13 Tool Shaping / Helper Tools V1 Design Packet

Why this wave mattered:

- all three are now evidence-backed rather than speculative
- all three directly affect the next real SNC version boundary
- they reduce the chance of building the wrong long-term substrate

### Wave B: Remaining High-Value Repo Breadth

Status:

- completed

4. `06` CC-04 Remote / Server / Service Layer
5. `07` CC-05 Governance / Settings / Policy
6. `05` CC-03 Command / Product Shell Atlas

Why:

- these packets help separate runtime donor value from product-shell/service/governance effects
- they are valuable, but less immediately blocking than wave A

### Wave C: Later Product-Feel Completion

Status:

- completed

7. `01` OC-06 UI / Product Surfaces
8. `09` CC-07 Secondary Intelligence Layers

Why:

- useful for later SNC productization and output-density/anti-mechanical work
- lower leverage than durable memory, orchestration, and tool-shaping design

## Best Thread Bundles

Recommended combined claims for one external main thread:

- `06 + 07`
  - service architecture plus governance/policy
  - good fit because both distinguish runtime behavior from outer control layers

- `05 + 09`
  - command shell plus product-feel/secondary intelligence
  - good fit because both are about "why CC feels good" without being core runtime harness

Recommended solo claims:

- `01`

Those should stay solo because each can easily sprawl and each feeds a different SNC decision lane.

## Practical Dispatcher Guidance

If you are opening only 3 external threads now:

- wait for a fresh numbered packet set from the dispatcher

If you are opening 5 external threads now:

- wait for a fresh numbered packet set from the dispatcher

If you are opening 6 external threads now:

- wait for a fresh numbered packet set from the dispatcher

## Output Rule

External threads should still claim from:

- `research/53_external_thread_claims.md`

This phase note is only the dispatcher's recommended launch order and bundling guidance.
